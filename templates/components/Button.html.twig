<{{ tag }} {{ attributes.defaults({
    class: this.sizeClasses~' '~this.variantClasses~' font-medium rounded-lg focus:outline-none transition-colors',
}) }}>{% block content %}{% endblock %}</{{ tag }}>
