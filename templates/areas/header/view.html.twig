{% extends 'link-wrapper.html.twig' %}

{% block component %}
    {% set header = pimcore_input('header') %}
    {% set headerValue = header.getValue() %}

    {% if editmode %}
        {{ header|raw }}
    {% elseif headerValue %}

    <twig:Ui:Header
    :variantValues="variantValues"
    :additionalClasses="additionalClasses"
    tag="h1"
    >
    {{ headerValue }}
    </twig:Ui:Header>`

    {% endif %}
{% endblock %}