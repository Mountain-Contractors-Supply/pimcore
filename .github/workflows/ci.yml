name: CI

on:
  pull_request:
    types: [opened, reopened, synchronize]

concurrency:
  group: ci__${{ github.ref }}
  cancel-in-progress: true

jobs:
  CI:
    uses: TorqIT/pimcore-github-actions-workflows/.github/workflows/ci-docker.yml@fix/ci-checkout-submodules
    permissions:
      contents: read
      actions: read
    with:
      GITHUB_APP_CHECKOUT: true
      SUBMODULES: true
    secrets:
      GITHUB_APP_ID: ${{ secrets.GH_APP_ID }}
      GITHUB_APP_PRIVATE_KEY: ${{ secrets.GH_APP_PRIVATE_KEY }}
      BUILD_TIME_SECRETS: >
        {
          "pimcore-product-key": "${{ secrets.CI_PIMCORE_PRODUCT_KEY }}",
          "pimcore-instance-identifier": "${{ secrets.CI_PIMCORE_INSTANCE_IDENTIFIER }}",
          "pimcore-encryption-secret": "${{ secrets.CI_PIMCORE_ENCRYPTION_SECRET }}"
        }
