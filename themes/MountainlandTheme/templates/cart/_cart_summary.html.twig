<aside class="cart-sidebar">
    <div class="card">
        <h2>Order Summary</h2>
        <div class="summary-row">
            <span>Subtotal</span>
            <span id="summary-subtotal">${{ cart.subTotal|number_format(2, '.', ',') }}</span>
        </div>
        <div class="summary-row">
            <span>Shipping</span>
            <span id="summary-shipping" style="font-style: italic; font-size: 0.9rem;">Calculated on next page</span>
        </div>
        <div class="summary-row">
            <span>Tax (est.)</span>
            <span id="summary-tax">${{ cart.tax|number_format(2, '.', ',') }}</span>
        </div>
        <hr />
        <div class="summary-row total">
            <span>Total</span>
            <span id="summary-total">${{ cart.totalDue|number_format(2, '.', ',') }}</span>
        </div>

        {% if not cart.empty %}
        <a href="{{ path('checkout') }}" class="btn-primary full" data-turbo-frame="_top">Proceed to Checkout</a>
        {% endif %}

        <a href="{{ path('root_category') }}" class="btn-secondary full" data-turbo-frame="_top">Continue Shopping</a>
    </div>
    <a href="#" class="return-policy-link">View Return Policy</a>
</aside>