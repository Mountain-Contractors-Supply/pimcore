{% extends 'default/full-width.html.twig' %}

{% block main_body %}
    <turbo-frame id="cart-list-frame"
                 src="{{ path('cart_list_partial') }}"
                 loading="lazy"
                 data-cart-list-url-value="{{ path('cart_list_partial') }}">
        {% include 'cart/_cart_list_frame.html.twig' with { 'cart': cart } %}
    </turbo-frame>
{% endblock %}
