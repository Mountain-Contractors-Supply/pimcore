{% extends 'layout.html.twig' %}

{% block head %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/style.css') }}" />
    <link rel="stylesheet" href="{{ asset('css/home.css') }}" />
{% endblock %}

{% block content %}
    <twig:PartFinder />

    <turbo-frame id="category-frame" data-turbo-action="advance" autoscroll data-autoscroll-block="start" data-autoscroll-behavior="smooth" style="display: block; margin-bottom: 20px;">
        <div class="bg-white border-b border-gray-200">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
                <twig:Navigation:Breadcrumb />
            </div>
        </div>

        <div class="divisions-container product-listing-container">
            <div class="product-listing-layout">
                <aside class="product-filter-sidebar" aria-label="Product Filters">
                    <twig:Category:FilterBox actionPath="{{ app.request.pathinfo }}" turboFrame="category-frame" />
                </aside>

                <div class="product-listing-results">
                    {% set currentSort = app.request.query.get('sort', 'relevance') %}
                    {% set currentQuery = app.request.query.all %}

                    <div class="product-sort-toolbar">
                        <form
                            method="get"
                            action="{{ app.request.pathinfo }}"
                            class="product-sort-form"
                            data-turbo="true"
                            data-turbo-action="advance"
                            data-turbo-frame="category-frame"
                            data-controller="filter-box"
                            data-action="change->filter-box#submitNow"
                        >
                            {% for key, value in currentQuery %}
                                {% if key not in ['sort', 'page'] %}
                                    {% if value is iterable %}
                                        {% for item in value %}
                                            <input type="hidden" name="{{ key }}[]" value="{{ item }}" />
                                        {% endfor %}
                                    {% else %}
                                        <input type="hidden" name="{{ key }}" value="{{ value }}" />
                                    {% endif %}
                                {% endif %}
                            {% endfor %}

                            <label for="product-sort-select">Sort By</label>
                            <select id="product-sort-select" name="sort">
                                <option value="relevance" {% if currentSort == 'relevance' %}selected{% endif %}>Relevance</option>
                                <option value="name-asc" {% if currentSort == 'name-asc' %}selected{% endif %}>Name (A-Z)</option>
                                <option value="name-desc" {% if currentSort == 'name-desc' %}selected{% endif %}>Name (Z-A)</option>
                                <option value="price-asc" {% if currentSort == 'price-asc' %}selected{% endif %}>Price (Low to High)</option>
                                <option value="price-desc" {% if currentSort == 'price-desc' %}selected{% endif %}>Price (High to Low)</option>
                                <option value="newest" {% if currentSort == 'newest' %}selected{% endif %}>Newest</option>
                            </select>
                        </form>
                    </div>

                    <section class="product-category-subheader" aria-labelledby="product-category-title">
                        <h2 id="product-category-title">Product Category</h2>
                        <p>
                            Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
                            Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.
                        </p>
                    </section>

                    <div class="divisions-grid" style="grid-template-rows: none;">
                        {% set items = products.pagination.items %}
                        {% set pagination = products.pagination %}
                        {% for item in items %}
                            <twig:Card>
                                <a href="{{ element_path(item) }}" data-turbo-frame="_top">{{ item.name }}</a>

                                <twig:Product:ProductPrice
                                    :productId="item.productId"
                                    uom="ea"
                                />

                                <twig:Product:ProductAvailability
                                    :productId="item.productId"
                                    uom="ea"
                                />

                                <div data-controller="cart" data-cart-product-id-value="{{ item.productId }}" data-cart-uom-value="ea">
                                    <div class="my-4 space-y-3">
                                        <div>
                                            <twig:QuantityInput :value="1" :min="1" name="quantity" data-cart-target="qtyInput" />
                                        </div>

                                        <div class="flex items-stretch gap-3 flex-nowrap">
                                            <twig:Product:ProductAddToCart text="Add to Cart" class="flex-1 min-w-0" />
                                            <twig:Product:ProductAddToList />
                                        </div>
                                    </div>

                                    <div class="hidden text-sm text-gray-600 -mt-2 mb-4" data-cart-target="inCartMessage"></div>
                                </div>
                            </twig:Card>
                        {% endfor %}
                    </div>

                    {% if pagination %}
                        <twig:Navigation:PaginationControls
                            :pagination="pagination"
                            :limit="app.request.query.get('limit', 10)"
                        />
                    {% endif %}
                </div>
            </div>
        </div>
    </turbo-frame>
{% endblock %}
