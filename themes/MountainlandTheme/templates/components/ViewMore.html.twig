<div class="view-toggle-container" style="width: 100vw; position: relative; left: 50%; right: 50%; margin-left: -50vw; margin-right: -50vw; margin-top: 2rem;">
    <button class="view-toggle-btn" 
            style="background-color: var(--primary-color); color: white; border: none; padding: 1rem 3rem; font-size: 1.1rem; font-family: var(--primary-font); font-weight: 600; cursor: pointer; border-radius: 0; transition: all 0.3s ease; width: 100%;"
            onmouseover="this.style.backgroundColor='#0a0d3d'" 
            onmouseout="this.style.backgroundColor='var(--primary-color)'"
            data-toggle-grid-target="viewToggleBtn" 
            data-action="click->{{ controllerName }}#toggleGrid">
        VIEW MORE ▼
    </button>
</div>
